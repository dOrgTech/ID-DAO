pragma solidity ^0.5.0;

// TODO: RegistryReputationClaim (if user is any of the registries, they can claim rep)
contract HumanReputationClaim {
  // TODO:
  // - universal scheme
  // - construct w/ address of Human Registry
  // - register params (avatar=>amount)
}
